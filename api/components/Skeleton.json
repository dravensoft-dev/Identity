{
  "component": "Skeleton",
  "description": "A loading placeholder that reserves the space real content will take.",
  "api": {
    "variant": { "form": "enum", "type": "SkeletonVariant", "default": "block",
                 "description": "The shape the placeholder reserves." },
    "width": { "form": "primitive", "type": "string",
               "description": "CSS width, e.g. \"100%\" or \"12rem\". Defaults to full width." },
    "height": { "form": "primitive", "type": "string",
                "description": "CSS height. Defaults per variant." },
    "lines": { "form": "primitive", "type": "number",
               "description": "Number of rows when variant=\"text\". The last runs short." },
    "radius": { "form": "primitive", "type": "string",
                "description": "CSS border radius. Defaults to a small token radius." }
  }
}
