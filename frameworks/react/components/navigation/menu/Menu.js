/* GENERATED by scripts/build-demos.mjs — edit the .jsx sibling, not this file.
 * Bun.Transpiler, classic JSX (React.createElement). See build-demos.mjs
 * for the full rationale. */
import React, { useState, useRef, useEffect } from "react";
let injected = false;
function useMenuKeyframes() {
  useEffect(() => {
    if (injected || typeof document === "undefined")
      return;
    injected = true;
    const s = document.createElement("style");
    s.setAttribute("data-arena-menu", "");
    s.textContent = "@keyframes arena-menu{from{opacity:0;transform:translateY(calc(var(--sp-1) * -1))}to{opacity:1;transform:none}}" + "@media (prefers-reduced-motion:reduce){@keyframes arena-menu{from{opacity:0}to{opacity:1}}}";
    document.head.appendChild(s);
  }, []);
}
export function Menu({ trigger, items, align = "start", onSelect }) {
  if (items == null)
    throw new Error("Menu: `items` is required");
  if (!React.isValidElement(trigger) || trigger.type === React.Fragment) {
    throw new Error("Menu: `trigger` must be a single element that forwards props to its focusable control. " + "A fragment or a bare string takes aria-haspopup and aria-expanded nowhere.");
  }
  useMenuKeyframes();
  const [open, setOpen] = useState(false);
  const ref = useRef(null);
  const panelRef = useRef(null);
  const triggerEl = () => ref.current && ref.current.firstElementChild;
  const close = (restoreFocus) => {
    setOpen(false);
    const el = triggerEl();
    if (restoreFocus && el && typeof el.focus === "function")
      el.focus();
  };
  useEffect(() => {
    if (!open)
      return;
    const onDoc = (e) => {
      if (ref.current && !ref.current.contains(e.target))
        setOpen(false);
    };
    const onKey = (e) => {
      if (e.key === "Escape")
        close(true);
    };
    document.addEventListener("mousedown", onDoc);
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("mousedown", onDoc);
      document.removeEventListener("keydown", onKey);
    };
  }, [open]);
  useEffect(() => {
    if (!open)
      return;
    const first = panelRef.current && panelRef.current.querySelector('[role="menuitem"]:not([disabled])');
    if (first)
      first.focus();
  }, [open]);
  const run = (it) => {
    if (it.disabled)
      return;
    close(true);
    onSelect && onSelect(it);
  };
  useEffect(() => {
    const el = triggerEl();
    if (!el || typeof el.setAttribute !== "function")
      return;
    el.setAttribute("aria-haspopup", "menu");
    el.setAttribute("aria-expanded", String(open));
  }, [open]);
  const decoratedTrigger = React.cloneElement(trigger, {
    "aria-haspopup": "menu",
    "aria-expanded": open,
    onClick: (e) => {
      if (trigger.props.onClick)
        trigger.props.onClick(e);
      setOpen((v) => !v);
    }
  });
  return React.createElement("div", {
    ref,
    style: { position: "relative", display: "inline-flex" }
  }, decoratedTrigger, open && React.createElement("div", {
    role: "menu",
    ref: panelRef,
    style: {
      position: "absolute",
      top: "calc(100% + calc(var(--sp-1) * 1.5))",
      [align === "end" ? "right" : "left"]: 0,
      zIndex: "var(--z-dropdown)",
      minWidth: "calc(var(--sp-1) * 50)",
      padding: "calc(var(--sp-1) * 1.5)",
      background: "var(--surface-card)",
      border: "var(--bw) solid var(--line-strong)",
      borderRadius: "var(--r-md)",
      boxShadow: "var(--shadow-2)",
      animation: "arena-menu var(--dur-fast) var(--ease-out)"
    }
  }, items.map((it, i) => {
    if (it.divider)
      return React.createElement("div", {
        key: i,
        style: { height: "var(--bw)", background: "var(--color-base-300)", margin: "calc(var(--sp-1) * 1) 0" }
      });
    if (it.header)
      return React.createElement("div", {
        key: i,
        style: { fontFamily: "var(--font-mono)", fontSize: "var(--dz-text-2xs)", letterSpacing: "var(--ls-field-label)", textTransform: "uppercase", color: "var(--mute)", padding: "calc(var(--sp-1) * 2) calc(var(--sp-1) * 2.5) calc(var(--sp-1) * 1)" }
      }, it.header);
    return React.createElement(MenuRow, {
      key: i,
      item: it,
      onRun: () => run(it)
    });
  })));
}
function MenuRow({ item, onRun }) {
  const [hover, setHover] = useState(false);
  const color = item.destructive ? "var(--danger)" : "var(--bone-dim)";
  const bg = hover && !item.disabled ? item.destructive ? "var(--danger-soft)" : "var(--crimson-soft)" : "transparent";
  return React.createElement("button", {
    role: "menuitem",
    onClick: onRun,
    disabled: item.disabled,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "flex",
      alignItems: "center",
      gap: "calc(var(--sp-1) * 2.5)",
      width: "100%",
      textAlign: "left",
      padding: "calc(var(--sp-1) * 2) calc(var(--sp-1) * 2.5)",
      border: "none",
      borderRadius: "var(--r-sm)",
      cursor: item.disabled ? "not-allowed" : "pointer",
      background: bg,
      color: item.disabled ? "var(--mute)" : hover && !item.destructive ? "var(--crimson)" : color,
      opacity: item.disabled ? 0.6 : 1,
      fontFamily: "var(--font-body)",
      fontSize: "var(--fs-md)"
    }
  }, item.icon && React.createElement("i", {
    className: item.icon,
    "aria-hidden": "true",
    style: { fontSize: "var(--icon-md)", display: "inline-flex" }
  }), React.createElement("span", {
    style: { flex: 1 }
  }, item.label), item.shortcut && React.createElement("span", {
    style: { fontFamily: "var(--font-mono)", fontSize: "var(--dz-text-xs)", color: "var(--mute)" }
  }, item.shortcut));
}
