{
  "cases": [
    {
      "name": "clickable",
      "when": "actionsEnabled is false or absent -- the chip root itself renders as <button>",
      "pattern": "button",
      "exceptions": []
    },
    {
      "name": "clickable-with-actions",
      "when": "actionsEnabled is true -- a kebab cannot nest inside a button, so the root becomes a <div> and the interactive attributes move down to the chip BODY, a descendant <button>",
      "pattern": "button",
      "exceptions": []
    }
  ]
}
