import { arenaStyles } from '../../../ArenaStyles.generated';
import manifest from '../arena-calendar/ArenaCalendar.classes.generated';

export const arenaCalendarEventStyles = arenaStyles(manifest);
