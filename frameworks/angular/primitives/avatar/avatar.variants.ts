import { tv } from '../../../tailwind/tv';
import manifest from '../../../tailwind/components/Avatar.manifest';

/** Recipe for `arena-avatar`. See `tag.variants.ts` for why this
 *  extensionless import resolves to the generated `.ts`, not the `.json`. */
export const avatarStyles = tv(manifest);
