import { tv } from '../../../tailwind/tv';
import manifest from '../../../tailwind/components/ConfirmDialog.manifest';

/** Recipe for `arena-confirm-dialog`. See `tag.variants.ts` for why this
 *  extensionless import resolves to the generated `.ts`, not the `.json`. */
export const confirmDialogStyles = tv(manifest);
