name: Arena main

on:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: main
  cancel-in-progress: false

env:
  BUN_VERSION: 1.3.14
  CHROME_PATH: /usr/bin/google-chrome

jobs:
  verify:
    name: build and test everything
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: bun-cache-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-cache-${{ runner.os }}-
      - run: bun install --frozen-lockfile

      - name: build
        run: bun run build

      - name: the build is idempotent, so a tracked file it rewrote is a generator out of step
        run: git diff --exit-code

      - name: assemble both packages, so check:packages reads two manifests rather than none
        run: bun run build:packages

      - name: every gate
        run: bun scripts/check/arena/check-all.ts --no-tests

      - name: the whole suite, counted per domain
        run: bun run ci:summarize
