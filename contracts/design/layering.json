{
  "z": {
    "$type": "number",
    "$description": "Layering — a system-wide invariant (what covers what), not geometry. The\nfamily declares the order; the values only have to preserve it. Before this\nit was five magic numbers in five files: Menu and Tooltip both sat at 900,\nso a tooltip on a menu item resolved by DOM order rather than by design;\nDialog and ConfirmDialog both sat at 1000, and ConfirmDialog opens from a\nDialog, so it worked only by accident of mount order; Toast, the one thing\nthat must float above everything, declared nothing at all. The order, least\nto most interruptible: nav < dropdown < tooltip < modal < modal-nested <\npalette < onboarding < toast.",
    "nav": { "$value": 800, "$description": "Fixed page navigation — a bottom bar, a sticky top bar. Arena draws none of these; the slot exists because the invariant is system-wide and a consumer's own shell is part of the system. Below dropdown, so a Menu opened from the bar covers it rather than resolving by DOM order, which is the failure this family was created to end" },
    "dropdown": { "$value": 900, "$description": "Menu's panel — a dropdown anchored to its trigger" },
    "tooltip": { "$value": 950, "$description": "above dropdown, so a tooltip on a menu item wins" },
    "modal": { "$value": 1000, "$description": "Dialog" },
    "modal-nested": { "$value": 1050, "$description": "ConfirmDialog, which opens from a Dialog" },
    "palette": { "$value": 1100, "$description": "CommandPalette" },
    "onboarding": { "$value": 1200, "$description": "the coachmark; the scrim beneath it derives at the point of use as calc(var(--z-onboarding) - 10) — one slot, two uses" },
    "toast": { "$value": 1300, "$description": "floats above everything, including onboarding — a transient notice raised by an action taken inside a dialog must stay visible" }
  }
}
