{
  "name": "figure-with-data-table",
  "source": "https://www.w3.org/WAI/WCAG22/Understanding/non-text-content.html",
  "description": "A chart. APG has no pattern for one, so this is Arena's own, adopted from WCAG 1.1.1 rather than invented: a picture nobody can read is not an alternative, so every chart pairs a labelled graphic with a real table of its numbers. The table is visually hidden, not absent. A role=\"img\" subtree is presentational, so nothing focusable inside the graphic reaches a screen reader however correct its ARIA; that one fact splits the pattern in two. A screen reader gets the table, which is already here. A sighted keyboard user gets a data cursor that moves exactly what hover moves: the emphasised mark, the crosshair and the tooltip, whose numbers were reachable by pointer alone (WCAG 2.1.1). A hidden listbox of options mirroring the marks was weighed and refused: it is a second copy of the same numbers in the same DOM, and one source per fact is the rule that copy would break. This describes what both chart layers already do, not an aspiration.",
  "requires": {
    "roles.graphic": "img",
    "roles.label": "aria-label naming the chart",
    "alternative.table": "a real <table> of the plotted numbers, visually hidden",
    "focus.roving": "one tab stop for the whole plot; the data cursor moves within it and never adds a second stop",
    "keyboard.ArrowLeft": "moves the data cursor to the previous point, clamping at the first",
    "keyboard.ArrowRight": "moves the data cursor to the next point, clamping at the last",
    "keyboard.Home": "moves the data cursor to the first point",
    "keyboard.End": "moves the data cursor to the last point",
    "keyboard.Escape": "clears the data cursor"
  }
}
