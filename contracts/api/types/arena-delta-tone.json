{
  "name": "ArenaDeltaTone",
  "kind": "enum",
  "description": "Whether a change was GOOD. Separate from ArenaDirection because revenue down is negative and latency down is positive, and only the product knows which. Every sign renders outline, never filled.",
  "values": ["neutral", "positive", "negative"]
}
