{
  "name": "ArenaTableCellLayout",
  "kind": "enum",
  "description": "How an ArenaTable column's cells render in card mode: `row` is a label/value pair, `block` is full width with no label. Checked against every enum already declared here; nothing carries this pair. ArenaSkeletonVariant is the only other declaration containing the value `block`, and it names a shape of placeholder rather than a layout.",
  "values": ["row", "block"]
}
