{
  "name": "ArenaPointSeries",
  "kind": "object",
  "description": "One plotted series of pairs: two parallel measurements, read at the same index. It is a separate type from ArenaSeries rather than a variant of it, because the two disagree about what a mark IS: an ArenaSeries value takes its place on the axis from its index, and a pair carries both coordinates. It is two arrays rather than an array of {x, y} because R1 refuses an array of objects, and the pairing by index is the same one every chart here already makes between labels and values, with the same rule when the two do not line up.",
  "fields": {
    "label": {
      "form": "primitive",
      "type": "string",
      "required": true,
      "description": "Names this series, for its rows in the accessible table and for a legend. Required and guarded for the reason ArenaSeries.label is: nothing can derive what a series is about."
    },
    "x": {
      "form": "array",
      "of": "number",
      "required": true,
      "description": "The horizontal values, in order. Continuous rather than an index, which is what a scatter needs and what a chart spacing its marks by position cannot give."
    },
    "y": {
      "form": "array",
      "of": "number",
      "required": true,
      "description": "The vertical values, in order, paired with `x` at the same index. A mark is drawn only where both arrays have a value, so the shorter one ends the series: a pair with half a coordinate is not a point, and inventing the other half is the one thing a chart may not do."
    },
    "slot": {
      "form": "primitive",
      "type": "number",
      "description": "One identity colour from the categorical ramp for the whole series. 1-based, clamped to the ramp, never cycled. Absent, the chart assigns the series its position in the list."
    },
    "tone": {
      "form": "enum",
      "type": "ArenaSeriesTone",
      "description": "Semantic colour, for a series that IS a state. Mutually exclusive with slot; passing both warns in development and tone wins."
    }
  }
}
