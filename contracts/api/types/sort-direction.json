{
  "name": "SortDirection",
  "kind": "enum",
  "description": "Which way a column is ordered. Deliberately not Direction, which is a delta's arrow: up and down say which way a NUMBER moved, and ascending and descending say how a COLUMN is arranged. One set of names for both would read as the same idea in the two places it is not.",
  "values": ["asc", "desc"]
}
